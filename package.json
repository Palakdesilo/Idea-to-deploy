{
  "name": "idea-to-deploy-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build:types": "npm run build --workspace=@idea-to-deploy/types",
    "build:api": "npm run build --workspace=@idea-to-deploy/api",
    "build:web": "npm run build --workspace=@idea-to-deploy/web",
    "build": "npm run build:types && npm run build --workspaces --if-present"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}