from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from database import engine, Base
try:
    from auth_routes import router as auth_router
except ImportError:
    auth_router = None
from routes.landing_routes import router as landing_router
from routes.user_routes import router as user_router
from routes.account_routes import router as account_router
from routes.alert_routes import router as alert_router

# Create database tables
if engine:
    Base.metadata.create_all(bind=engine)

app = FastAPI(
    title="A futuristic logistics platform for asteroid minin... API",
    description="Generated by Idea-to-Deploy",
    version="1.0.0"
)

# CORS middleware
app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:3000"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

# Include routers
if auth_router:
    app.include_router(auth_router)

app.include_router(landing_router)
app.include_router(user_router)
app.include_router(account_router)
app.include_router(alert_router)

@app.get("/")
async def root():
    return {"message": "Welcome to A futuristic logistics platform for asteroid minin... API"}

@app.get("/health")
async def health():
    return {"status": "healthy"}
